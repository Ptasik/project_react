
let initialState = {
    friends: [
        {id: 1, name: 'Ihor'},
        {id: 2, name: 'Dadik'},
        {id: 3, name: 'Mira'},
    ]
}

const friendsReducer = (state = initialState, action) => {

    return state;
}

export default friendsReducer;